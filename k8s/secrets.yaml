apiVersion: v1
kind: Secret
metadata:
  name: taskqueue-secrets
  namespace: taskqueue
type: Opaque
stringData:
  DATABASE_URL: "postgresql://taskqueue:taskqueue@postgres-service:5432/taskqueue"
  POSTGRES_PASSWORD: "taskqueue"
